
        # new_route_customers = new_route.customers
        # # check if there's any customer belong to this route
        # for customer in new_route_customers:
        #     if customer in self.customers:
        #         #check if its an interior or not
        #         if not customer in get_interior_customers():
        #             # add the new route to the common customer
        #             common_customer = customer
        #             common_customer_idx = self.customers.index(common_customer)
        #             self.customers = self.customers[:common_customer_idx] +new_route_customers+ self.customers[common_customer_idx:]
        #             return True
        #         else:
        #             print("interior customers cannot be combined")
        #             return False
                
        # self.customers += new_route_customers
        
